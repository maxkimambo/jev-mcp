{
  "name": "jev-mcp",
  "version": "0.13.0",
  "description": "Let TypeSafe Jev read files, logs and search results so your coding agent reads only the answer. For Claude Code, Codex and pi.",
  "type": "module",
  "license": "MIT",
  "author": "Max Kimambo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxkimambo/jev-mcp.git"
  },
  "homepage": "https://github.com/maxkimambo/jev-mcp#readme",
  "bugs": {
    "url": "https://github.com/maxkimambo/jev-mcp/issues"
  },
  "bin": {
    "jev-mcp": "bundle/index.js"
  },
  "files": [
    "bundle",
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "typesafe",
    "jev",
    "system-one",
    "classification",
    "confidence",
    "ai",
    "pi-package"
  ],
  "scripts": {
    "build": "tsc && npm run bundle",
    "prepublishOnly": "npm run typecheck && npm test",
    "start": "node bundle/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "test:e2e": "node --test test/e2e.test.mjs",
    "bundle": "esbuild src/index.ts src/cli.ts src/pi.ts --bundle --platform=node --format=esm --target=node20 --outdir=bundle --log-level=warning --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.12"
  },
  "publishConfig": {
    "access": "public"
  },
  "pi": {
    "extensions": [
      "./bundle/pi.js"
    ],
    "skills": [
      "./skills"
    ]
  }
}
